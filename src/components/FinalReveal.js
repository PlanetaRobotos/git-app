import React from 'react';

const FinalReveal = ({ message }) => (
  <div className="final-reveal">
    <h2>Final Challenge</h2>
    <p>{message}</p>
    <p>Find the main graffiti sketch to unlock your prize!</p>
  </div>
);

export default FinalReveal;
